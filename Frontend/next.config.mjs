// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Enables React strict mode
    // Add other configurations here
  };
  
  export default nextConfig; // Export as default for ES Modules